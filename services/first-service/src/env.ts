import dotenv from "dotenv";
dotenv.config();

// Eventually this file will contain all runtime configuration.
// For now, I'm adding this basic version so that env files
//  get loaded for all modules
